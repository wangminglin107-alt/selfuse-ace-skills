# Security Policy

V1 denies network access unless a request and a declared provider both allow it. Research
artifacts must remain inside the resolved project root, and secrets must never be written to
artifacts, checkpoints, logs, or fixtures.

Report suspected path traversal, credential exposure, unsafe subprocess execution, or provenance
loss before using the affected workflow with sensitive research data.

